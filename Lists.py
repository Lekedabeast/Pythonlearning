name = ["John", "Bob", "Lekaka", "Lukaku", "Tim", "Kolade"]
name[0] = "jon" # this changes the name in the terminal if someone has made a mistake
print(name) # this right here picks the word according to their index position
# there are negative indexes where if u do a negative it goes from the end of the list
# [] this is only used to find something in index order
# to get a range of data [0:3] discludes the last index
