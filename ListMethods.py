numbers = [1, 2, 3, 4, 5,]
# numbers.insert(0, -1) # first number in brackets shows the index of where you want to place that object, second can be anything float, boolean, Integer
# numbers.clear() -- clears the whole list
# numbers.remove(3) removes the number three in the list
# numbers.append(4) -- this is to add one number to the end
# print(1 in numbers) # this is a boolean expression to see whether 1 is in the data of numbers
print(len(numbers)) # returns the number of elements in a list